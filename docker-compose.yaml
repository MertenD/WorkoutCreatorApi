services:

  workout-creator-api:
    build:
      dockerfile: Dockerfile
    image: mertend/workout-creator-api:latest
    container_name: merten-workout-creator-api
    restart: unless-stopped
    env_file:
      - .env
    labels:
      - traefik.enable=true
      - traefik.http.routers.workout-creator-api.rule=Host(`www.workout-creator-api.mertendieckmann.de`) || Host(`workout-creator-api.mertendieckmann.de`)
      - traefik.http.routers.workout-creator-api.entrypoints=websecure
      - traefik.http.routers.workout-creator-api.tls=true
      - traefik.http.routers.workout-creator-api.tls.certresolver=lets-encrypt
      - traefik.docker.network=web
    networks:
      - web

networks:
  web:
    external: true
